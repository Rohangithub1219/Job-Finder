{
  "name": "json-server",
  "version": "1.0.0",
  "description": "A JSON Server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server db.json --port $PORT"
  },
  "author": "Chirag Raj",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
